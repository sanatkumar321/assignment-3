#include <iostream>
using namespace std;

int main() {
	int a,b;
	cout<< "enter two numbers : ";
	cin >> a>>b ;
	if (a>b)
	{
		if (a%b==0)
		cout <<"LCM = "<<a;
		else
		cout <<"LCM = "<<a*b;
	}
	else
	{
		if (b%a==0)
		cout <<"LCM = "<<b;
		else 
		cout <<"LCM = "<<a*b;
	}
	
	return 0;
}
